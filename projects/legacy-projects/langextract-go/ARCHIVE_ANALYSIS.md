# langextract-go/ — Archive Analysis
**Archived:** March 9, 2026

Cloned Go langextract repository (had its own .git/ directory). Full source code with cmd/, internal/, docs/, examples/, Dockerfile, and Go modules.

**Replaced by:** The actual running service is Docker container `phase66-langextract` on port 8095. This directory was a reference clone, not the deployed service.

**Files:** Go source, Dockerfile, docs, examples (143 files)
